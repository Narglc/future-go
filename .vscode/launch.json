{
    // 使用 IntelliSense 了解相关属性。 
    // 悬停以查看现有属性的描述。
    // 欲了解更多信息，请访问: https://go.microsoft.com/fwlink/?linkid=830387
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Package",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/gomicro_demo/helloworld/main.go",
            "env": {},
            "args": [
                // "-f", "${workspaceFolder}/src/whatsapp/etc/config.yaml",
                // //"-db-address", "file:${workspaceFolder}/mdtest.db?_foreign_keys=on",
                // "-debug","true"
            ],
            "showLog": true
            // "program": "${fileDirname}"          // 使用单文件时，使用此处配置
        }
    ]
}